def think(text):
    """
    Fallback responder used whenever BB doesn't recognize an intent
    through the normal rule-based system. This is a placeholder that
    can later be wired up to a real AI/LLM API for smarter replies.
    """
    return "I'm still learning that one, Boss."
